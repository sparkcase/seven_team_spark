package com.ganjunhua.holiday.json

import java.io.File



object JsonTest {
  def main(args: Array[String]): Unit = {
    /* case class Person(id: String, name: String, age: Int)
     val allPerson = fromJson[List[Person]](FileUtils.readFileToString(new File("data/json/test.json"), "UTF-8"))

     allPerson.foreach(println)
     */

  }
}
